<Page x:Class="Goman_WPF_PROJ_UP02.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Goman_WPF_PROJ_UP02"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Авторизация">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">

            <TextBlock Text="Авторизация" 
                       HorizontalAlignment="Center" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,20"/>

            <TextBlock Text="Логин:"/>
            <TextBox x:Name="TxtLogin" Width="260" HorizontalAlignment="Left"/>

            <TextBlock Text="Пароль:"/>
            <PasswordBox x:Name="PswPassword" Width="260" HorizontalAlignment="Left"/>

            <Button Content="Войти" 
                    Click="BtnLogin_Click" 
                    Margin="0,20,0,0" 
                    Width="150" 
                    HorizontalAlignment="Center"/>
            <Button Content="Войти без регистрации" 
                    Click="BtnLoginWithoutAuth_Click" 
                    Margin="0,20,0,0" 
                    Width="200" 
                    HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
